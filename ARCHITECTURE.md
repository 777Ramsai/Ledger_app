# 🏗️ Pocket Ledger Architecture

## System Architecture

```
┌─────────────────────────────────────────────────────────┐
│                    USER BROWSER                         │
│            (Streamlit Web Interface)                    │
└────────────────────┬────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────┐
│                  STREAMLIT APP                          │
│                    (app.py)                             │
├─────────────────────────────────────────────────────────┤
│  Authentication Layer                                   │
│  ├── render_auth_page()        → Login/Signup UI       │
│  ├── register_user()           → Create account        │
│  ├── login_user()              → Verify credentials    │
│  ├── hash_password()           → SHA-256 hashing       │
│  └── Session Management        → User state tracking   │
└────────┬────────────────────────┬──────────────────────┘
         │                        │
         ▼                        ▼
┌──────────────────┐    ┌─────────────────────┐
│  CREDENTIALS DB  │    │   USER LEDGERS      │
│                  │    │                     │
│ users_data.json  │    │ user_data/          │
│                  │    │ ├── alice_*_ledger  │
│ {                │    │ ├── bob_*_ledger    │
│   "email": {     │    │ └── charlie_*_ledger│
│     "password"   │    │                     │
│     "created_at" │    │ CSV Format:         │
│   }              │    │ Date | Shop | Type  │
│ }                │    │ Amount | Description│
│                  │    │                     │
│ SHA-256 Hashed   │    │ Per-User Isolation  │
│ All Passwords    │    │ Complete Privacy    │
└──────────────────┘    └─────────────────────┘
```

---

## Authentication Flow

```
┌─────────────┐
│   START     │
└──────┬──────┘
       │
       ▼
┌──────────────────┐
│ User visits app  │
└──────┬───────────┘
       │
       ▼
┌────────────────────────┐
│ Authenticated?         │
└────┬──────────┬────────┘
     │NO        │YES
     ▼          ▼
  ┌──────────────┐  ┌────────────────┐
  │ Show Login/  │  │ Load User      │
  │ Signup Page  │  │ Dashboard      │
  └──┬───┬──────┘  └─────┬──────────┘
     │   │               │
  New Account?        ▼
     │ YES        ┌──────────────┐
     │            │ User selects │
     ▼            │ Shop & sees  │
  ┌──────────────┐ │ Transactions │
  │ Sign Up Form │ │ Add/View     │
  │ Email        │ │ Transactions │
  │ Password     │ │ PDF Export   │
  │ Confirm Pass │ └──┬───────────┘
  │             │    │
  │ Validate:   │    │ Logout?
  │ - Email fmt │    │ YES
  │ - Min 6 chr │    │
  │ - No duplice│    ▼
  └─┬──────────┘  ┌──────────────┐
    │             │ Clear session│
    ▼             │ Return to    │
  ┌──────────────┐ │ login screen │
  │ Store hash + │ └──────────────┘
  │ Create user  │
  └─┬────────────┘
    │
    ▼
  ┌──────────────┐
  │ Go to Login  │
  └──┬───────────┘
     │
     ▼
  ┌──────────────┐
  │ Login Form   │
  │ Email        │
  │ Password     │
  │             │
  │ Hash pass &  │
  │ Compare      │
  └──┬───┬──────┘
     │   │
  Match?│
     │ YES    │NO
     ▼        ▼
  ┌───────┐  ┌────────────┐
  │ Login │  │ Error msg: │
  │ OK!   │  │ Wrong      │
  └───────┘  └────────────┘
     │
     ▼
  ┌──────────────┐
  │ Set session  │
  │ authenticated│
  │ = True       │
  └──┬───────────┘
     │
     ▼
  ┌──────────────┐
  │ Load user    │
  │ ledger file  │
  └──┬───────────┘
     │
     ▼
  ┌──────────────┐
  │ Display      │
  │ Dashboard    │
  └──────────────┘
```

---

## Data Storage Flow

```
                    ┌─────────────────┐
                    │  User Login     │
                    │ email@example   │
                    │ password123     │
                    └────────┬────────┘
                             │
                    ┌────────▼─────────┐
                    │ Hash Password    │
                    │ SHA-256          │
                    │ xyz789...        │
                    └────────┬─────────┘
                             │
                ┌────────────┼────────────┐
                │                        │
                ▼                        ▼
         ┌────────────────┐     ┌──────────────────┐
         │ Check if exists│     │ Store credentials│
         │ in users_data  │     │ users_data.json  │
         └────┬───────────┘     └──────────────────┘
              │                          ▲
         ┌────┼─────────────────────────┘
         │    │
    exists?   does not exist
         │    │
      YES     NO
         │    │
         ▼    ▼
      ┌──────────────┐
      │ Verify Hash  │
      │ vs stored    │
      │ hash         │
      └──┬───────┬───┘
         │       │
       Match?   Mismatch
         │       │
      YES        NO
         │       │
         ▼       ▼
      ┌─────┐  ┌──────────┐
      │LOGIN│  │ ERROR    │
      │  OK  │  │ Message  │
      └──┬──┘  └──────────┘
         │
         ▼
    ┌──────────────────────┐
    │ Generate user file   │
    │ path from email:     │
    │ email@example.com    │
    │ ↓                    │
    │ email_example_com_   │
    │ ledger.csv          │
    └────────┬─────────────┘
             │
             ▼
    ┌──────────────────────┐
    │ Load/Create ledger   │
    │ in user_data/        │
    │ Directory            │
    └────────┬─────────────┘
             │
             ▼
    ┌──────────────────────┐
    │ User can add         │
    │ transactions to      │
    │ their file only      │
    └──────────────────────┘
```

---

## Data Isolation Model

```
┌─────────────────────────────────────────────────────────┐
│                    ROOT DIRECTORY                       │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  ┌──────────────────────┐  ┌──────────────────────┐   │
│  │  users_data.json     │  │   user_data/         │   │
│  │                      │  │                      │   │
│  │  All user creds      │  │  ┌────────────────┐  │   │
│  │  (hashed passwords)  │  │  │ alice_email    │  │   │
│  │                      │  │  │ com_ledger.csv │  │   │
│  │  Cannot copy data    │  │  │                │  │   │
│  │  between users       │  │  │ Alice's data   │  │   │
│  │                      │  │  │ only visible   │  │   │
│  └──────────────────────┘  │  │ to Alice       │  │   │
│                            │  └────────────────┘  │   │
│                            │  ┌────────────────┐  │   │
│                            │  │ bob_yahoo      │  │   │
│                            │  │ com_ledger.csv │  │   │
│                            │  │                │  │   │
│                            │  │ Bob's data     │  │   │
│                            │  │ only visible   │  │   │
│                            │  │ to Bob         │  │   │
│                            │  └────────────────┘  │   │
│                            │  ┌────────────────┐  │   │
│                            │  │ charlie_mail   │  │   │
│                            │  │ com_ledger.csv │  │   │
│                            │  │                │  │   │
│                            │  │ Charlie's data │  │   │
│                            │  │ only visible   │  │   │
│                            │  │ to Charlie     │  │   │
│                            │  └────────────────┘  │   │
│                            │                      │   │
│                            │  No cross-user       │   │
│                            │  access possible     │   │
│                            └──────────────────────┘   │
│                                                         │
└─────────────────────────────────────────────────────────┘

Each user:
✓ Only sees their own ledger file
✓ Cannot access other users' files
✓ Has private transaction history
✓ Has isolated balance calculations
```

---

## Password Security Architecture

```
┌──────────────────────────────────────────────┐
│           PASSWORD FLOW                      │
└──────────────────────────────────────────────┘

SIGNUP PROCESS:
┌──────────────────────────────────────────────┐
│ User enters: MySecurePass123                 │
└────────────────┬─────────────────────────────┘
                 │
                 ▼
        ┌────────────────────┐
        │ SHA-256 Hash       │
        │ Algorithm          │
        │                    │
        │ Converts to:       │
        │ a7c6f8a2e1b3...   │
        └────────────────────┘
                 │
                 ▼
        ┌────────────────────────────────┐
        │ Store in users_data.json:      │
        │ {                              │
        │   "email@example": {           │
        │     "password_hash":           │
        │       "a7c6f8a2e1b3...",      │
        │     "created_at": "2026-01-28" │
        │   }                            │
        │ }                              │
        └────────────────────────────────┘

IMPORTANT:
❌ Original password NOT stored
❌ Hash cannot be reversed
✓ Same password always = same hash
✓ Different passwords = different hashes

LOGIN PROCESS:
┌──────────────────────────────────────┐
│ User enters: MySecurePass123         │
└────────────────┬──────────────────────┘
                 │
                 ▼
        ┌─────────────────────┐
        │ Hash this password  │
        │ SHA-256             │
        │ Get: a7c6f8a2e1b3   │
        └────────────────────┘
                 │
                 ▼
        ┌───────────────────────────────┐
        │ Compare with stored hash      │
        │ from users_data.json          │
        └───────────┬───────────────────┘
                    │
            ┌───────┴────────┐
            │                │
         MATCH            NO MATCH
            │                │
            ▼                ▼
        ┌────────┐      ┌──────────┐
        │ LOGIN  │      │ ERROR    │
        │ OK     │      │ Wrong    │
        │        │      │ password │
        └────────┘      └──────────┘
```

---

## Security Measures Summary

```
┌─────────────────────────────────────────────────┐
│       SECURITY LAYERS IMPLEMENTED               │
└─────────────────────────────────────────────────┘

Layer 1: INPUT VALIDATION
├─ Email format check (@. required)
├─ Password length check (6+ chars)
├─ Duplicate email prevention
└─ Type checking for all inputs

Layer 2: PASSWORD SECURITY
├─ SHA-256 hashing algorithm
├─ No plain-text storage
├─ One-way encryption
└─ Hash verification on login

Layer 3: DATA ISOLATION
├─ Per-user ledger files
├─ Email-based file naming
├─ Sandboxed file access
└─ No cross-user data access

Layer 4: SESSION MANAGEMENT
├─ Streamlit session state
├─ Browser-based auth
├─ Automatic logout
└─ No persistent tokens

Layer 5: FILE PROTECTION
├─ .gitignore excludes credentials
├─ Separate user_data directory
├─ Protected users_data.json
└─ No hardcoded secrets
```

---

## User Journey Map

```
┌──────────────────────────────────────────────────┐
│         USER JOURNEY - FIRST TIME                │
└──────────────────────────────────────────────────┘

STAGE 1: AWARENESS
    ↓
    User hears about Pocket Ledger
    
STAGE 2: INSTALLATION
    ↓
    pip install -r requirements.txt
    streamlit run app.py
    
STAGE 3: SIGNUP
    ↓
    ├─ Click "Sign Up" tab
    ├─ Enter email: user@company.com
    ├─ Create password: SecurePass123
    ├─ Confirm password: SecurePass123
    └─ Click "Sign Up"

STAGE 4: LOGIN
    ↓
    ├─ Click "Login" tab
    ├─ Enter email: user@company.com
    ├─ Enter password: SecurePass123
    └─ Click "Login"

STAGE 5: DASHBOARD ACCESS
    ↓
    Dashboard loads with:
    ├─ Total Payable metric
    ├─ Shop summary table
    └─ Add Transaction form

STAGE 6: ADD TRANSACTIONS
    ↓
    ├─ Click "Add New Transaction"
    ├─ Fill form (date, amount, shop, type)
    └─ Click "Save Entry"

STAGE 7: VIEW REPORTS
    ↓
    ├─ Select shop from dropdown
    ├─ View transaction history
    ├─ See running balance
    └─ Download PDF statement

STAGE 8: EXPORT & BACKUP
    ↓
    ├─ Click "Download CSV Backup"
    └─ Data downloaded to device

STAGE 9: LOGOUT
    ↓
    ├─ Click "Logout" in sidebar
    └─ Return to login screen

STAGE 10: RETURN VISIT
    ↓
    Previous data is available
    Login shows all previous transactions
    Continue managing ledger
```

---

## Technology Stack

```
┌─────────────────────────────────────────────┐
│         TECHNOLOGY ARCHITECTURE             │
└─────────────────────────────────────────────┘

FRONTEND
├─ Streamlit 1.28.0+
│  ├─ Web interface
│  ├─ Form handling
│  └─ Session management
└─ HTML5/CSS (Streamlit renders)

BACKEND
├─ Python 3.8+
│  ├─ Core logic
│  └─ File I/O
├─ Pandas 2.0.0+
│  ├─ Data manipulation
│  ├─ CSV handling
│  └─ Statistics
└─ FPDF2 2.7.0+
   ├─ PDF generation
   ├─ Report formatting
   └─ Statement creation

SECURITY
├─ hashlib (SHA-256)
│  └─ Password hashing
├─ json
│  └─ User credentials storage
└─ Session state
   └─ Authentication management

STORAGE
├─ JSON Files
│  └─ users_data.json (credentials)
├─ CSV Files
│  └─ {email}_ledger.csv (transactions)
└─ File System
   └─ user_data/ directory

DEPLOYMENT OPTIONS
├─ Local (Current)
├─ Streamlit Cloud
├─ Docker container
├─ Traditional server
└─ Cloud platforms (AWS/Azure/GCP)
```

---

**Architecture designed for:**
- ✓ Security (SHA-256 hashing)
- ✓ Privacy (Data isolation)
- ✓ Scalability (File-based to database migration ready)
- ✓ Usability (Simple, clean interface)
- ✓ Maintainability (Well-documented code)
